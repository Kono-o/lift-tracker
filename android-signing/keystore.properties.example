# Copy to keystore.properties and fill in after running setup-android-signing.fish
# Back up lift-tracker-release.keystore securely — you need the same key for future APK updates.

storeFile=lift-tracker-release.keystore
storePassword=your_store_password
keyAlias=lift-tracker
keyPassword=your_key_password